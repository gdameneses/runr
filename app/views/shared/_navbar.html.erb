<nav class="navbar navbar-light bg-light">
  <div class="container-fluid d-flex">
    <a class="navbar-brand" href="#"><h1>RUNR</h1></a>
    <div class="nav-menu">
      <% if user_signed_in? %>
      <%= link_to "Dashboard", dashboard_path, class: 'm-2'%>
      <%= link_to "Manage your restaurant", restaurant_path(current_user.restaurant) if current_user.restaurant  %>
      <%= link_to "Log out", destroy_user_session_path, method: :delete, class: 'm-2' %>
      <% else %>
      <%= link_to "Log in", new_user_session_path %>
      <% end %>
    </div>
  </div>

</nav>
